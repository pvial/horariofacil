<!-- Validation failure messages -->
<% if @colegio.present? %>
  <% if @colegio.errors.any? %>
    <% @colegio.errors.full_messages.each do |message| %>
      <div class="alert alert-danger">
        <%= message %>
      </div>
    <% end %>
  <% end %>
<% end %>

<h1>
  Add a new colegio
</h1>

<hr>

<form action="/create_colegio" method="post">
  <!-- Label and input for nombre -->
  <div class="form-group">
    <label for="nombre">
      Nombre
    </label>

    <input type="text" id="nombre" name="nombre" class="form-control" value="<%= @colegio.try(:nombre) %>">
  </div>

  <button class="btn btn-block btn-outline-secondary">
    Create colegio
  </button>
</form>

<hr>

<a href="/colegios">
  Go back
</a>
